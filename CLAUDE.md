# CLAUDE.md

## Shell Preference

- This is a Windows environment. Use PowerShell as the primary shell.
- Only use Bash for commands that are inherently POSIX-only (sed, specific Go toolchain quirks) and note the reason.

## Project

Casdoor — open-source IAM / SSO server. Go backend (Beego), React frontend (Ant Design).
