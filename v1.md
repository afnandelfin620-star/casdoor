# Casdoor MySQL → PostgreSQL 迁移方案

## Context

Casdoor 当前通过 cfgserver（gRPC）获取数据库配置，但代码中硬编码了 MySQL。需要切换到 PostgreSQL。不保留 MySQL 兼容。

Casdoor 与 gcc 等微服务的关键区别：Casdoor 使用 xorm 的 `Sync2()` 创建/修改表结构（DDL），因此 PostgreSQL role 需要 DDL 权限。Casdoor 已有 PostgreSQL 驱动支持（`object/ormer.go`），只需修改 cfgserver 集成层。

## 修改的文件（共2个）

### 1. `conf/cftp_conf.go`

**`GetDatabaseDSN()`** — 生成 PostgreSQL 格式 DSN：
- 地址从 `POSTGRES_ADDR` 环境变量读取（host:port 格式）
- fallback：K8s 服务发现 `pgbouncer-external.{namespace}.svc.cluster.local:6432`
- DSN 格式（key=value，匹配 xorm/lib/pq）：
  ```
  user=xxx password=xxx host=xxx port=6432 sslmode=verify-full sslrootcert=/etc/tls/ca.crt dbname=xxx
  ```
- TLS：pgbouncer 强制要求 TLS，读取 `TLS_DIR` 环境变量获取 CA 证书路径（与已有的 `getCfgServerTransportCreds()` 模式一致）
- 删除 MySQL 格式的 `%s:%s@tcp(%s)/`

**函数/变量重命名**：去掉 mysql 前缀（如 `mySqlAddress` → `pgAddress`）

### 2. `conf/conf.go`

**`GetConfigString()` 中 driverName**：从 `"mysql"` 改为 `"postgres"`

## 不需要修改的文件

- `object/ormer.go` — 已有 PostgreSQL 支持
- `conf/config.proto` / protobuf 生成文件 — 不变
- `conf/app.conf` — 不改，cfgserver 覆盖
- `CftpConfig` 结构体 — 不需要新增字段，DriverName 不需要从 cfgserver 获取（硬编码）

## cfgserver 配置 JSON

字段不变（无需新增 DriverName）：
```json
{
    "Database": "casdoor",
    "DBUser": "casdoorsys",
    "DBPassword": "your_secure_password",
    "RedisPassword": "..."
}
```

## PostgreSQL Role 和 Database 创建

Casdoor 需要 DDL 权限（CREATE TABLE, ALTER TABLE, CREATE INDEX 等），因为 `object/ormer.go` 中 `createTable()` 调用 `xorm.Engine.Sync2()` 同步所有表结构。

```sql
-- 1. 创建数据库（由 postgres 管理员账号执行）
CREATE DATABASE casdoor;

-- 2. 创建带有 DDL 权限的业务层用户（由 postgres 管理员账号执行）
CREATE USER casdoorsys WITH PASSWORD 'your_secure_password';

-- 3. 授予连接数据库权限
GRANT CONNECT ON DATABASE casdoor TO casdoorsys;

-- =========================================================================
-- 以下命令必须先连接到 casdoor 数据库执行
-- \c casdoor;
-- =========================================================================

-- 4. 授予 schema 完全控制权（Casdoor 需要 CREATE 建表）
GRANT ALL PRIVILEGES ON SCHEMA public TO casdoorsys;

-- 5. 授予对当前所有表和序列的全部权限
GRANT ALL PRIVILEGES ON ALL TABLES IN SCHEMA public TO casdoorsys;
GRANT ALL PRIVILEGES ON ALL SEQUENCES IN SCHEMA public TO casdoorsys;

-- 6. 设定默认特权：未来创建的表和序列自动授权
ALTER DEFAULT PRIVILEGES IN SCHEMA public GRANT ALL PRIVILEGES ON TABLES TO casdoorsys;
ALTER DEFAULT PRIVILEGES IN SCHEMA public GRANT ALL PRIVILEGES ON SEQUENCES TO casdoorsys;
```

与 gcc 微服务的权限对比：
| 权限 | gcc 等微服务 | casdoor |
|------|-------------|---------|
| CONNECT | ✓ | ✓ |
| USAGE on schema | ✓ | ✓ |
| CREATE on schema | **REVOKED** | **GRANTED** |
| ALTER DEFAULT PRIVILEGES | 仅 CRUD | **ALL PRIVILEGES** |

## K8s 环境变量

- `POSTGRES_ADDR=pgbouncer-external.mynamespace.svc.cluster.local:6432`
- `CFGSERVER_ADDR=...`（已有，不变）
- `TLS_DIR=/etc/tls`（已有，用于 TLS 证书）

## 验证方式

1. PostgreSQL 中执行上述 SQL 创建 role 和 database
2. 在 cfgserver 中配置 casdoor 的 JSON（driverName 字段不需要，代码硬编码）
3. 确保 `POSTGRES_ADDR` 环境变量指向正确的 PostgreSQL
4. 编译启动 casdoor，观察表同步（Sync2）是否成功
5. 调用 casdoor API 验证 CRUD 操作
6. PostgreSQL 中 `\dt` 查看表是否正确创建
